__pycache__/
*.py[cod]
.venv/
venv/
*.db
.pytest_cache/
.DS_Store

# Real credentials — .env.example is the one that is committed.
.env

# Dependencies, wherever a runner decides to put them. A run that installs
# into the checkout would otherwise show every library file as part of the
# change someone is reviewing.
.pylibs/
